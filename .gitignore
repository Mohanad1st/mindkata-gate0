node_modules/
.next/
out/
coverage/
playwright-report/
test-results/
.artifacts/
.env
.env.*
!.env.example
.DS_Store
*.log
*.tsbuildinfo
.claude/settings.local.json

# GSD planning workspace. Internal working notes: this repository is a published
# artifact, and planning state is not part of what a reader should have to wade
# through. Use `gsd-pr-branch` if planning commits ever need filtering.
.planning/

.vercel
